{
  "name": "playwright-cucumber-playwright",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "test:e2e": "npm run build && cucumber-js --require dist/cucumber.conf.js --require dist/acceptance/stepDefinitions/**/*.js --format @cucumber/pretty-formatter",
    "test:normal": "npm run build && cucumber-js --require dist/cucumber.conf.js --require dist/acceptance/stepDefinitions/**/*.js --format @cucumber/pretty-formatter acceptance/features/*.feature",
    "test:all": "npm run build && cucumber-js --require dist/cucumber.conf.js --require dist/acceptance/stepDefinitions/**/*.js -f json:cucumber_report.json --publish-quiet acceptance/features/*.feature",
    "report": "npm run build && node dist/reporter.js"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.9.0",
    "@cucumber/pretty-formatter": "^3.3.1",
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "cucumber-html-reporter": "^7.2.0",
    "dotenv": "^17.4.2",
    "playwright": "^1.60.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "semver": "^7.5.3"
  }
}
